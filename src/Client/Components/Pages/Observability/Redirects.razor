@page "/observability/{Provider}"

@inject NavigationManager Navigation
@inject IOptionsSnapshot<DocsSiteOptions> DocsSiteOptions

@code
{
    [Parameter] public string Provider { get; set; }

    protected override void OnParametersSet()
    {
        Navigation.NavigateTo($"{DocsSiteOptions.Value.BaseAddress}/guides/observability/{Provider}.html");
    }
}
